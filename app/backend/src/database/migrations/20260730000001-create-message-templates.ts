import { DataTypes, QueryInterface } from "sequelize";
const table = { tableName: "MessageTemplates", schema: "messaging" };
module.exports = { up: async (q: QueryInterface): Promise<void> => { await q.createTable(table, { id: { type: DataTypes.UUID, primaryKey: true, allowNull: false }, companyId: { type: DataTypes.INTEGER, allowNull: false }, publicId: { type: DataTypes.UUID, allowNull: false }, name: { type: DataTypes.STRING(120), allowNull: false }, content: { type: DataTypes.TEXT, allowNull: false }, variables: { type: DataTypes.JSONB, allowNull: false, defaultValue: [] }, version: { type: DataTypes.INTEGER, allowNull: false, defaultValue: 1 }, active: { type: DataTypes.BOOLEAN, allowNull: false, defaultValue: true }, createdBy: { type: DataTypes.INTEGER, allowNull: true }, createdAt: { type: DataTypes.DATE, allowNull: false }, updatedAt: { type: DataTypes.DATE, allowNull: false } }); await q.addIndex(table, ["companyId", "publicId"], { unique: true, name: "message_templates_company_public_unique" }); }, down: async (q: QueryInterface): Promise<void> => { await q.dropTable(table); } };
